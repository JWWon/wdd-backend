version: '3.1'

services:
  db:
    image: mysql
    restart: always
    expose:
      - '3306'
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: dnjswldns96

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
